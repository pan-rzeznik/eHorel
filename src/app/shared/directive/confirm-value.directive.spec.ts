import { ConfirmValueDirective } from './confirm-value.directive';

describe('ConfirmValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmValueDirective();
    expect(directive).toBeTruthy();
  });
});
